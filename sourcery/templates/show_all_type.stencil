// Found {{ types.all.count }} Types
// {% for type in types.all %} {{ type.name }}  {% endfor %}

/**
 * Found {{ types.implementing.CICOAutoCodable.count }} CICOAutoCodable Types
 *{% for type in types.implementing.CICOAutoCodable %} {{ type.name }}  {% endfor %}
 */
